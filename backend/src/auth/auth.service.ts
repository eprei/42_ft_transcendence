import { Injectable } from '@nestjs/common';

@Injectable()
export class AuthService {
    // async validateUser(profile: any): Promise<any> {
    //     let user = { id: profile.id, username: profile.username };
    //     return user;
    // }
}
